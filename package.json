{
  "name": "filterinvites",
  "version": "1.0.0",
  "description": "Filter the content of a channel to only allow messages containing invite links for the current discord",
  "main": "filterinvites.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Hybris95",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^12.5.1",
    "dotenv": "^8.2.0"
  }
}
